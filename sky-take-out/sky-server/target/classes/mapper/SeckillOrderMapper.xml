<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.SeckillOrderMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into seckill_order (order_number, seckill_id, item_id, user_id, dish_id, dish_name, quantity, seckill_price, total_amount, address_id, remark, status, pay_deadline, create_time)
        values (#{orderNumber}, #{seckillId}, #{itemId}, #{userId}, #{dishId}, #{dishName}, #{quantity}, #{seckillPrice}, #{totalAmount}, #{addressId}, #{remark}, #{status}, #{payDeadline}, #{createTime})
    </insert>

    <update id="updateStatus">
        update seckill_order
        set status = #{status}, update_time = now()
        where order_number = #{orderNumber}
    </update>

</mapper>

