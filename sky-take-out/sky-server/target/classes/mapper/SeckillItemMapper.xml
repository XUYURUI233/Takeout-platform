<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.SeckillItemMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into seckill_item (seckill_id, dish_id, dish_name, original_price, seckill_price, seckill_stock, current_stock, limit_per_user, status, create_time)
        values (#{seckillId}, #{dishId}, #{dishName}, #{originalPrice}, #{seckillPrice}, #{seckillStock}, #{currentStock}, #{limitPerUser}, #{status}, #{createTime})
    </insert>

    <update id="update">
        update seckill_item
        <set>
            <if test="dishName != null">dish_name = #{dishName},</if>
            <if test="originalPrice != null">original_price = #{originalPrice},</if>
            <if test="seckillPrice != null">seckill_price = #{seckillPrice},</if>
            <if test="seckillStock != null">seckill_stock = #{seckillStock},</if>
            <if test="currentStock != null">current_stock = #{currentStock},</if>
            <if test="limitPerUser != null">limit_per_user = #{limitPerUser},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

</mapper>

